// here are some examples to show you how JS2Blockly works.
// not every JavaScript feature is represented here, I only had 5-6 months to realise it. I'll add these features one day when I'll have time.
// but feel free to pick up some lines of JS code in `misc/tests.txt` and have a look at what it does when "blocklyfied" ;-)

let p,r
const m=5

if (p>5) {
    while (p>5 || p<3) {
        r = true || 3 > 5 + (2 + x)
    }
}

for (i=0 ; i<5 ; i++) {}
for (let i=0 ; i<5 ; i++) {}
for (i=0 ; i<5 ; i+=3) {}